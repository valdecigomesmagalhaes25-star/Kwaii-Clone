
{
  "name": "kwai-clone-rewards",
  "version": "1.0.0",
  "description": "Kwai Clone with Monetization",
  "main": "index.tsx",
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "lucide-react": "^0.562.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "scripts": {
    "build": "mkdir -p www && cp index.html index.tsx metadata.json types.ts App.tsx constants.tsx www/ && cp -r components www/ 2>/dev/null || true",
    "ionic:build": "npm run build",
    "ionic:cap-add-android": "npx cap add android",
    "postinstall": "npx cap add android || echo 'Android platform already exists'"
  }
}
